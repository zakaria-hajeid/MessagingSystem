/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ReplayAllHiddenDirective } from './ReplayAllHidden.directive';

describe('Directive: ReplayAllHidden', () => {
  it('should create an instance', () => {
    const directive = new ReplayAllHiddenDirective();
    expect(directive).toBeTruthy();
  });
});
